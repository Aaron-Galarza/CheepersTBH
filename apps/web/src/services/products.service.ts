import apiClient from './api';

export const productsService = {
  getAll: async () => {
    // Implementar en bloque 2
    return [];
  },

  getById: async (id: string) => {
    // Implementar en bloque 2
    return null;
  },
};
